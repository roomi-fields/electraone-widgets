node_modules/
.DS_Store
docs/previews/
docs/lib
docs/widgets

# RTFM local database
.rtfm/

# docs.electra.one local mirror: track the extracted markdown so RTFM
# can index it; ignore the raw HTML (regeneratable, ~5 MB).
.electra-docs/html/
